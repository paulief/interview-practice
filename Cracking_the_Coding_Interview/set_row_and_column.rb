# Write an algorithm such that if an element in an MxN matrix is 0, its entire row and
# column is set to 0

def set_row_and_column(matrix, m, n)
	
	(0..m).each do |y|
		(0..n).each do |x|
			if (matrix[m][n] == 0)

		end
	end
end
